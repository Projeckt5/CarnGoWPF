<UserControl x:Class="CarnGo.HeaderBarControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CarnGo.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border Background="{StaticResource BlueMainBrush}" Height="40">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <!--Car'n'Go logo home button-->
            <Button Grid.Column="0"
                    Style="{StaticResource IconGrowButton}"
                    Content="{StaticResource FontAwesomeHomeIcon}"
                    Foreground="{StaticResource BackgroundMainBrush}"
                    FontSize="{StaticResource FontSizeLarge}"
                    />
            <!--Search bar-->

            <!--Profile icon-->
            <Button Grid.Column="2"
                    Style="{StaticResource IconGrowButton}"
                    Content="{StaticResource FontAwesomeUserIcon}"
                    FontSize="{StaticResource FontSizeLarge}"
                    />
            
            <!--Notification icon-->
            <Button Grid.Column="3"
                    Style="{StaticResource IconGrowButton}"
                    Content="{StaticResource FontAwesomeBellIcon}"
                    FontSize="{StaticResource FontSizeLarge}"/>


        </Grid>
    </Border>
</UserControl>
